<UserControl x:Class="Wpf.Demo.Views.UserControls.ToolBarUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ToolBarTray>
        <ToolBar>
            <Button Content="One"/>
            <Button Content="Two" IsEnabled="False"/>
            <Separator/>
            <Button Content="three"/>
        </ToolBar>
        <ToolBar>
            <ToggleButton Content="One" IsChecked="True"/>
            <ToggleButton Content="Two" IsEnabled="False"/>
            <Separator/>
            <ToggleButton Content="three"/>
        </ToolBar>
        <ToolBar>
            <RadioButton GroupName="t-one" Content="One" IsChecked="True"/>
            <RadioButton GroupName="t-one" Content="Two" IsEnabled="False"/>
            <Separator/>
            <RadioButton GroupName="t-one" Content="three"/>
        </ToolBar>
    </ToolBarTray>
</UserControl>
